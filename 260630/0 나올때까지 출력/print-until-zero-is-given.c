#include <stdio.h>

int main() {
    // Please write your code here.
    int val;
    while(1){
        scanf("%d", &val);
        if (val == 0) break;
        printf("%d\n", val);
    }
    return 0;
}